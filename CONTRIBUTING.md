# 参与欧洲时空地图

欢迎历史读者、研究者、地图制作者、设计师和开发者一起建设。一次贡献可以小到补正一个地名或一条出处。

## 不写代码也能参与

在 [Issues](https://github.com/xiongyi1990/european-history-map/issues/new/choose) 选择“史实与地图纠错”或“功能建议”。请写明年代、地点、当前显示、预期修改，并提供可核查的书目页码或公开链接。链接到地图的 `?year=843&place=...` 地址有助于复现。

只提出问题也可以；没有把握的结论请标为待核查。讨论围绕证据和地图表达，尊重不同专业背景的参与者。

## 提交代码或数据

1. Fork 仓库，克隆自己的 Fork，并从 `main` 创建主题分支。
2. 按 [README](README.md) 安装依赖。修改尽量集中在一个问题。
3. 修改历史数据时补充来源；修改行为时运行相关测试。
4. 执行 `npm test` 和 `npm run build`。
5. 提交 Pull Request，说明问题、改动、验证方式。界面改动请附前后截图。

无需另外签署 CLA。提交自己有权提供的内容；原创贡献按本项目 GPL-3.0-only 发布，第三方材料保留其原始授权和署名。

## 历史资料的记录方式

- 年份用整数：公元前为负数，公元后为正数，没有公元 0 年。地点记录的有效期不得无依据地延伸。
- 经度在前、纬度在后，使用 WGS84；城市代表点不是城市边界。
- 区分政权归属、实际控制、文化关联、居民群体和语言。居民用有依据的族群、语言、宗教或政治身份描述，不把现代“人种”分类直接套进古代疆界。
- 区分“史料有明确记载”“学界推断”“大致范围”“未知”。争议年代、路线或边界保留不确定性。
- 朝代存续时间不等于在每块地域的统治时间。新增帝国时，应尽量写明组成地域、获得或失去的时间，以及今天的位置。
- 优先使用原始文献、学术出版物、博物馆、考古机构及可靠地名数据库。网页链接之外，也记录能定位的章节或页码。
- 课程和书籍可作为阅读线索，不能把课程观点直接标成公认史实。独立概括，勿上传付费课程、整本书、未经授权配图或大段摘录。
- 修改 GeoJSON 时保留来源、许可证、适用年代和精度说明；不能用现代国界自动填充古代疆界。

## 新增一个地点的常见路径

1. 参照 `src/europe/turn-of-millennium.ts` 或对应时期模块，添加 `GazetteerPlace`：稳定 ID、中文名、古今异名、代表坐标与定位来源。
2. 添加该时期的 `HistoricalDetail`：`from` / `to`、政权、地域、居民、语言、地名说明。每条事实使用可解析的来源 ID；未知项明确写未知。
3. 在 `src/europe/course-sources.ts` 注册必要来源，并检查 `course-details.ts`、`history-details.ts` 中的汇总路径。
4. 如需出现在年代导览中，补充对应时期的导览配置，确保引用的地点 ID 存在且在该年有适用资料。
5. 运行相应时期测试及全量检查，手动打开 `?year=...&place=...`，检查详情、定位和来源链接。

数据结构以当前 TypeScript 类型和同目录测试为准。不要把自动测试通过当成史实已确认。

## 文件与部署

`artifacts/`、课程原文、个人路径、密钥、构建产物和大型生成缓存不应进入提交。地形缓存可通过 README 中的脚本重建；需要增加公共数据时先说明规模、来源与授权。

合并后的代码由维护者安排上线。现有演示站的托管配置不代表贡献者获得该站点部署权限；你可以使用自己的静态托管服务预览 Fork。
